mixin button(args)
  - let type = args.type || 'button'
  - let text = args.text || 'More datails'

  button(type=type, class!=attributes.class, id!=attributes.id)
    span= text

//- ! Usage +button({text:'More datails', type: "Submit"})(class='button panel__button')
